/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ExpenseResponseDto } from '../../models/expense-response-dto';

export interface TripsControllerExpenses$Params {
  tripId: string;
}

export function tripsControllerExpenses(http: HttpClient, rootUrl: string, params: TripsControllerExpenses$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ExpenseResponseDto>>> {
  const rb = new RequestBuilder(rootUrl, tripsControllerExpenses.PATH, 'get');
  if (params) {
    rb.path('tripId', params.tripId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ExpenseResponseDto>>;
    })
  );
}

tripsControllerExpenses.PATH = '/trips/{tripId}/expenses';
